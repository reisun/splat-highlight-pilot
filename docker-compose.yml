services:
  api:
    build: .
    ports:
      - "8030:8000"
    volumes:
      - shared-data:/shared-data
    env_file:
      - .env
    networks:
      - llm-network
    restart: unless-stopped

volumes:
  shared-data:
    external: true

networks:
  llm-network:
    external: true
